<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.src.appnivelestres20">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mdt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mdt"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppNivelEstres20">
        <activity android:name=".InicioApp"
            android:theme="@style/splashStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".examenTest.Result"
            android:theme="@style/sinBarRegistro"/>

        <activity android:name=".examenTest.Question"
            android:theme="@style/sinBarRegistro"/>

        <activity android:name=".MainActivity"
            android:theme="@style/sinBarRegistro"/>

        <activity android:name=".login.Register"
            android:theme="@style/sinBarRegistro"/>

        <activity android:name=".examenTest.ExamenEstress"
            android:theme="@style/sinBarRegistro"/>

        <activity android:name=".login.Login"
            android:theme="@style/sinBarRegistro">
        </activity>
        <activity android:name=".ayuda.Ayuda_Fragment"
            android:theme="@style/sinBarRegistro"/>
        <activity android:name=".contacto.TecnicasFragment"
            android:theme="@style/sinBarRegistro"/>
    </application>

</manifest>